extends base

//prepend content
    p before content

//append content
    p after content


// just 'block' will replace the block of any parent
block content
    p index.jade
    - site.posts.each(function(post) {
        include _partial/stub
    - })
