article
    p stub.jade what?
    h2
        a(href=config.root + post.path)= post.title
    ul
        - post.tags.each(function(tag) {
            li
                span
                    a(href=url_for(tag.path))
                        p= tag.name
        - })
